import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  tab: {
    backgroundColor: 'transparent',
  },
  indicator: {
    backgroundColor: '#BB7E8C',
  },
}))

export default useStyles
